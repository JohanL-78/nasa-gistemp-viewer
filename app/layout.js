import "./globals.css";

export const metadata = {
  title: "NASA GISTEMP Viewer",
  description: "Visualisation des données climatiques NASA",
};

export default function RootLayout({ children }) {
  return children;
}